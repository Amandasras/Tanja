---
const { variant, size } = Astro.props;
---

<button class={variant + " " + size}><slot /></button>

<style>
  .primary {
    border: 1.5px solid white;
    color: white;
    background-color: transparent;
    border-radius: 0.3rem;
    font-family: "montserrat", sans-serif;
    font-weight: 500;
    font-style: normal;
    cursor: pointer;
  }
  .secondary {
    border: none;
    color: white;
    background-color: #359dba;
    border-radius: 3rem;
    font-family: "montserrat", sans-serif;
    font-weight: 500;
    font-style: normal;
    cursor: pointer;
  }

  .small {
    font-size: 102%;
    width: 200px;
    height: 50px;
  }

  .medium {
    font-size: 102%;
    width: 200px;
    height: 50px;
  }

  .large {
    font-size: 102%;
    width: 240px;
    height: 50px;
  }
</style>

<script>
  // Find knappen ved hjælp af dens id
  var button = document.getElementById("shopAlt");

  // Tilføj en eventlytter til knappen
  button.addEventListener("click", function () {
    // Før brugeren videre til en anden side
    window.location.href = "../pages/produkter"; // Erstat med din ønskede URL
  });
</script>
